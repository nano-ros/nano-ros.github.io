---
title: Tx Standalone
description: Standalone mode where nano-ros runs without a host transport.
---

## Overview

Tx is the fully autonomous mode. It is useful when the system has no upstream host or when the local control loop must run by itself.

## Behavior

- no host transport
- local control and telemetry only
- FDIR still active
- safe hold still available
